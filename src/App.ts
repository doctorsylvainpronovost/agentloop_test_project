export const renderApp = (): string => {
  return `
    <section class="card">
      <h1>Weather App Scaffold</h1>
      <p>Frontend and backend scaffolding is ready for weather features.</p>
    </section>
  `;
};
